# knom-data-collecting-server

```bash
repo/
|- data/
|- client.html # jitsi collection system you can attach this code to jitsi/web index.html
|- data.zip # collected data set
|- docker-compose.yml # generating torture-selenium client with docker-compose
|- index.js # nodeJS server code.
|- loop.sh # generating torture-selenium client with looping command
|- package-lock.json
`- packege.json
```
